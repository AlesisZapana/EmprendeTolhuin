{% extends 'base.html.twig' %}

{% block title %}Ambito{% endblock %}

{% block body %}
    <h1>Ambito</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ambito.id }}</td>
            </tr>
            <tr>
                <th>Tipoambito</th>
                <td>{{ ambito.tipoambito }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ambito_index') }}">back to list</a>

    <a href="{{ path('ambito_edit', {'id': ambito.id}) }}">edit</a>

    {{ include('ambito/_delete_form.html.twig') }}
{% endblock %}
