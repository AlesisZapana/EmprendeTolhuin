{% extends 'base.html.twig' %}

{% block title %}Estado de Solicitud{% endblock %}

{% block body %}
    <h1>Estado de solicitud:{{estadosolicitud.nombre}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ estadosolicitud.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary btn-sm" href="{{ path('estadosolicitud_index') }}">Volver a lista</a>

    <a class="btn btn-primary btn-sm" href="{{ path('estadosolicitud_edit', {'id': estadosolicitud.id}) }}">Editar</a>

    {{ include('estadosolicitud/_delete_form.html.twig') }}
{% endblock %}
