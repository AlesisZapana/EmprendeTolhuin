{% extends 'base.html.twig' %}

{% block title %}Estadosolicitud{% endblock %}

{% block body %}
    <h1>Estadosolicitud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estadosolicitud.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estadosolicitud.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estadosolicitud_index') }}">back to list</a>

    <a href="{{ path('estadosolicitud_edit', {'id': estadosolicitud.id}) }}">edit</a>

    {{ include('estadosolicitud/_delete_form.html.twig') }}
{% endblock %}
