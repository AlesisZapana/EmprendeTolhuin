{% extends 'base.html.twig' %}

{% block title %}Sector Productivo{% endblock %}

{% block body %}
    <h1>Sector Productivo: {{sectorproductivo.nombre}} </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ sectorproductivo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary btn-sm" href="{{ path('sectorproductivo_index') }}">Volver a lista</a>

    <a class="btn btn-secondary btn-sm" href="{{ path('sectorproductivo_edit', {'id': sectorproductivo.id}) }}">Editar</a>

    {{ include('sectorproductivo/_delete_form.html.twig') }}
{% endblock %}
