{% extends 'base.html.twig' %}

{% block title %}Sectorproductivo{% endblock %}

{% block body %}
    <h1>Sectorproductivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sectorproductivo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sectorproductivo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sectorproductivo_index') }}">back to list</a>

    <a href="{{ path('sectorproductivo_edit', {'id': sectorproductivo.id}) }}">edit</a>

    {{ include('sectorproductivo/_delete_form.html.twig') }}
{% endblock %}
