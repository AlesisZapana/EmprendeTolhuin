{{ form_start(form) }}
    <div class="row">
    <div class="col col-lg-4 col-md-6 col-sm-12">
    {{ form_widget(form) }}
    </div>
    </div>
    <center>
    <div class="col-lg-12 col-md-8 col-sm-12">
    <div id="mapid"></div>
    </div>
    </center>
    <button class="btn btn-outline-primary">{{ button_label|default('Enviar') }}</button>
{{ form_end(form) }}
